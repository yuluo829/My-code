// package main		//插入排序

// import(
// 	"fmt"
// )

// func main(){
// 	array := [] int {5, 3, 2}
// 	res := inserSort(array)
// 	fmt.Println(res)
// }

// func inserSort(array [] int)[] int{
// 	n := len(array)
// 	for i:= 1; i < n; i++{
// 		for j := i; j > 0  && array[j] < array[j - 1]; j--{
// 			array[j], array[j - 1] = array[j - 1], array[j]
// 		}
// 	}
// 	return array
// }

package main

import (
	"fmt"
)

func main(){
	arr := {7, 8, 6, 2, 4, 3, 5, 1, 0, 9}
	fmt.Println(arr)
	for i := 1; i < n; i ++{
		for j := 1; j > 0 && arr[j]
	}
}